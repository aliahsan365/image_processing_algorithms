#include <iostream>
#include <map>
#include <vector>

using namespace std;

namespace Utils
{
  int  maxim_map(map<int,int>&);
  void map_percentage(map<int,int>&,int);
  void print_map(map<int,int>&);
  void print_matrix(vector<vector<int>> &);
}